const routes = {
    home: "/",
    login: "/login",
    register: "/register",
    producto: "/producto",
    bodega: "/bodega",
    

    admin: {
        usuario: "/admin/usuario",
    }

    // usuario = (id) => (id ? '/usuario/:${id}': 'usuario/id',
};

export default routes;