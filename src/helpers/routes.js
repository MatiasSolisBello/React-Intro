const routes = {
    home: "/",
    login: "/login",
    register: "/register",
    producto: "/producto",
    bodega: "/bodega",
    account: "/account",
    // usuario = (id) => (id ? '/usuario/:${id}': 'usuario/id',
    usuario: "/admin/usuario"
    

};

export default routes;