const routes = {
    home: "/",
    login: "/login",
    register: "/register",
    producto: "/producto",
    bodega: "/bodega",
    account: "/account",
    

    admin: {
        usuario: "/admin/usuario",
    }

    // usuario = (id) => (id ? '/usuario/:${id}': 'usuario/id',
};

export default routes;